# Quickstart

1. Start the FastAPI backend from the `backend` folder.
2. Start the Next.js frontend from the `frontend` folder.
3. Add your MP3 and JPG files into the backend media folders.
4. Open `http://localhost:3000/radio` to test playback.
